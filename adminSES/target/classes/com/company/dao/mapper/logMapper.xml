<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper는 여러 개가 존재할 수 있s으니 다른 maper아 중복이 되지 않도록 namespace를 선언 -->
<mapper namespace="lMap">
	<select id="getMemberLogList" parameterType="hashmap"
		resultType="com.company.dto.LogDTO">
		SELECT * FROM log WHERE M_ID LIKE #{mId} ORDER BY L_DATE DESC limit
		#{startNum}, #{ContentNum};
	</select>
	
	<select id="getMemberLListCnt" parameterType="String" resultType="int">
		SELECT count(*) FROM log WHERE M_ID LIKE #{mId};
	</select>
	
	<select id="getSearchedMemberLListCnt" parameterType="hashmap" resultType="int">
		SELECT count(*) FROM log WHERE M_ID LIKE #{mId} AND L_DATE BETWEEN #{lStartDT} AND #{lEndDT};
	</select>
	
	<select id="getSearchedMemberLList" parameterType="hashmap"
		resultType="com.company.dto.LogDTO">
		SELECT * FROM log WHERE M_ID LIKE #{mId} AND L_DATE BETWEEN #{lStartDT} AND #{lEndDT}
		ORDER BY L_DATE DESC limit
		#{startNum},
		#{ContentNum};
	</select>
</mapper>