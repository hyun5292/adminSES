<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper는 여러 개가 존재할 수 있으니 다른 maper아 중복이 되지 않도록 namespace를 선언 -->
<mapper namespace="eMap">
	<select id="ELogin" parameterType="String"
		resultType="com.company.dto.EmployeeDTO">
		SELECT * from employee
		WHERE M_ID like #{param1} AND M_PW IS NOT NULL;
	</select>

	<select id="GetSchEList" parameterType="hashmap"
		resultType="com.company.dto.EmployeeDTO">
		SELECT * FROM employee WHERE M_ID LIKE #{mId} AND M_NAME
		LIKE #{mName} ORDER BY M_NAME DESC limit #{startNum},
		#{ContentNum};
	</select>

	<select id="GetSchPageCnt" parameterType="hashmap"
		resultType="int">
		SELECT count(*) FROM employee WHERE M_ID LIKE #{mId} AND
		M_NAME LIKE #{mName};
	</select>

	<select id="GetEInfo" parameterType="String"
		resultType="com.company.dto.EmployeeDTO">
		SELECT
		* FROM employee WHERE M_ID = #{mId};
	</select>

	<update id="MakeDoAuth" parameterType="String">
		UPDATE employee SET E_AUTH = 'Y' WHERE M_ID = #{mId};
	</update>

	<update id="MakeDontAuth" parameterType="String">
		UPDATE employee SET E_AUTH = 'N' WHERE M_ID = #{mId};
	</update>
	
	<select id="GetsSchEmAdmin" parameterType="hashmap" resultType="com.company.dto.EmployeeDTO">
		SELECT * FROM employee WHERE M_ID LIKE #{mId} AND M_NAME LIKE #{mName} ORDER BY M_NAME DESC;
	</select>
	
	<select id="GetsSchEmACnt" parameterType="hashmap" resultType="int">
		SELECT count(*) FROM employee WHERE M_ID LIKE #{mId} AND M_NAME LIKE #{mName};
	</select>
</mapper>