<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper는 여러 개가 존재할 수 있으니 다른 maper아 중복이 되지 않도록 namespace를 선언 -->
<mapper namespace="qMap">
	<select id="getMemberQList" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE M_ID LIKE #{mId} ORDER BY Q_DATE
		DESC limit
		#{startNum},
		#{ContentNum};
	</select>

	<select id="getMemberQListCnt" parameterType="String"
		resultType="int">
		SELECT count(*) FROM qna WHERE M_ID LIKE #{mId};
	</select>

	<select id="getSearchedMemberQListCnt" parameterType="hashmap"
		resultType="int">
		SELECT count(*) FROM qna WHERE M_ID LIKE #{mId} AND Q_DATE
		BETWEEN #{qSDate} AND #{qEDate};
	</select>

	<select id="getSearchedMemberQList" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE M_ID LIKE #{mId} AND Q_DATE BETWEEN
		#{qSDate} AND #{qEDate}
		ORDER BY Q_DATE DESC limit
		#{startNum},
		#{ContentNum};
	</select>

	<select id="GetSnsQList" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE M_ID LIKE #{mId} ORDER BY Q_DATE
		DESC limit
		#{startNum},
		#{ContentNum};
	</select>

	<select id="GetSnsQListCnt" parameterType="String"
		resultType="int">
		SELECT count(*) FROM qna WHERE M_ID LIKE #{mId};
	</select>

	<select id="GetSchSnsQList" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE M_ID LIKE #{mId} AND Q_DATE BETWEEN
		#{StartDT} AND #{EndDT} ORDER BY Q_DATE DESC limit
		#{startNum},
		#{ContentNum};
	</select>

	<select id="GetSchSnsQListCnt" parameterType="String"
		resultType="int">
		SELECT count(*) FROM qna WHERE M_ID LIKE #{mId} AND Q_DATE
		BETWEEN #{StartDT} AND #{EndDT};
	</select>

	<select id="GetQRecentList" resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna ORDER
		BY Q_DATE DESC limit 0,4;
	</select>

	<select id="GetQListCnt" resultType="int">
		SELECT count(*) FROM qna;
	</select>

	<select id="GetQList" resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna ORDER BY
		Q_DATE DESC limit
		#{startNum},
		#{ContentNum};
	</select>

	<select id="GetSchQListCnt" parameterType="hashmap"
		resultType="int">
		SELECT count(*) FROM qna WHERE Q_DATE BETWEEN #{StartDT}
		AND #{EndDT};
	</select>

	<select id="GetSchQList" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE Q_DATE BETWEEN #{StartDT} AND
		#{EndDT} ORDER BY
		Q_DATE DESC limit
		#{startNum},
		#{ContentNum};
	</select>

	<select id="GetLastQnum" resultType="int">
		SELECT MAX(Q_NUM) FROM qna;
	</select>

	<select id="GetQData" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE Q_NUM=#{Qnum};
	</select>

	<update id="AddReply" parameterType="hashmap">
		UPDATE
		qna SET
		Q_REPLY =
		#{QReply} WHERE Q_NUM = #{Qnum};
	</update>

	<insert id="AddMewQna" parameterType="hashmap">
		INSERT INTO qna(Q_TITLE,
		M_ID, Q_PWD, Q_CONTENT, Q_DATE, Q_REPLY)
		VALUES
		(#{QTitle}, #{QWriter},
		#{QPwd}, #{QContent}, #{QnaDT}, #{QReply});
	</insert>

	<delete id="DeleteQna" parameterType="String">
		DELETE FROM qna WHERE Q_NUM
		= #{Qnum};
	</delete>

	<select id="GetSchKWQListCnt" parameterType="hashmap"
		resultType="int">
		SELECT count(*) FROM qna WHERE Q_TITLE LIKE #{title} AND
		M_ID LIKE #{qId};
	</select>

	<select id="GetSchKWQList" parameterType="hashmap"
		resultType="com.company.dto.QnaDTO">
		SELECT * FROM qna WHERE Q_TITLE LIKE #{title} AND M_ID LIKE
		#{qId} ORDER BY
		Q_DATE DESC limit
		#{startNum},
		#{ContentNum};
	</select>
</mapper>